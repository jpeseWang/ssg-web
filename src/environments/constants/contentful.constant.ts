import { environment } from '../environment';

export const CONTENTFUL_SPACE_ID = environment.contentfulService.spaceId;
export const CONTENTFUL_ACCESS_TOKEN =
  environment.contentfulService.accessToken;
